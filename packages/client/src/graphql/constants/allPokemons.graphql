query allPokemons{
    pokemons(limit: 20) {
        edges {
            cursor
            node {
                id
                name
                types
                classification
            }
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
