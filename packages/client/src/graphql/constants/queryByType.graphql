query FindPokemonByType($type: String!) {
    pokemonsByType(type: $type) {
        edges {
            node {
                id
                name
                classification
                types
            }
        }
    }
}
