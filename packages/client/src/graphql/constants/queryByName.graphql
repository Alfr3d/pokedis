query FindPokemonByName($name: String, $cursor: ID) {
    pokemons(q: $name, after: $cursor) {
        edges {
            node {
                id
                name
                types
            }
        }
        pageInfo {
            endCursor
            hasNextPage
        }
    }
}
