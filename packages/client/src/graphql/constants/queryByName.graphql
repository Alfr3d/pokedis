query FindPokemonByName($name: String) {
    pokemons(q: $name, limit: 20) {
        edges {
            node {
                id
                name
                types
            }
        }
    }
}
